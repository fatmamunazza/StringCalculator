package com.calculator.stringCal;

public class StringCalculator {
    public int add(String numbers) {
        int sum = 0;
        if (!numbers.isEmpty()) {
           //logic for addition
        }
        return sum;
    }
}
